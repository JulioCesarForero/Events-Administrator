# Nginx

Reverse proxy para servir SPA React y enrutar API FastAPI.

## Responsabilidades

- Servir archivos estaticos del frontend
- Fallback SPA (`/index.html`)
- Proxy de `/api` hacia backend
- Aplicar headers de seguridad basicos
