-- PostgreSQL 13+ incluye gen_random_uuid() en el núcleo.
-- Este archivo reserva extensiones opcionales para evolución (p. ej. pg_trgm para búsqueda).

-- CREATE EXTENSION IF NOT EXISTS pg_trgm;
